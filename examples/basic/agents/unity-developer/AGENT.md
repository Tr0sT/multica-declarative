Implement the assigned task directly.

Follow the project architecture and conventions. Compile the project and run the relevant tests before reporting completion. Report blockers explicitly instead of silently stopping.
